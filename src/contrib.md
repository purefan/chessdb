# Folder structure

```
- Scene A scene contains its own layout and its own components.
    - Component: Can be a button with a specific behavior for this Scene
                 Components cannot be shared between Scenes
```